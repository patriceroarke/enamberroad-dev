/**
 * @file
 * resource-search-toggle.js
 */

/**(function ($) {
    Drupal.behaviors.slider = {
        attach: function (context, settings) {
            processElements();
            $('.resource-search-desc').click(function(){
                href = $('.content').children('a').attr('href');
                window.location.href = href;
            });
        }
    };
  // my custom function to process elements
  function processElements(context) {

    // select elements within the context, filter out already processed ones
    // loop through remained (unprocessed) elements
    // '.my-class' - selector for the elements I want to process
    $('.resource-type-icon:not(.processed)').each(function(i,e){
      // mark the element as processed
      $(this).addClass('processed');
      $(this).hover(function(){
      div = $(this).parent();
      div.children('.resource-search-block').children('.resource-search-desc').toggle("slow");
      });

      // add process code here

    });
  }

  // run myProcessFunction on document.ready
  $(document).ready( function(){
        processElements();
  });

})(jQuery);

*/